import { AlertsFilterPipe } from './alerts-filter.pipe';

describe('AlertsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
